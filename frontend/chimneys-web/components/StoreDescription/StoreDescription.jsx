'use client'

import { useState } from "react";
import './StoreDescription.css';

export default function StoreDescription() {
    const [expanded, setExpanded] = useState(false);

    // Я трохи відформатував текст, додавши HTML теги для списків, щоб це виглядало гарно
    return (
        <section className="store-desc-section">
            <div className={`store-desc-content ${expanded ? 'expanded' : 'collapsed'}`}>
                <div className="store-desc-text">
                    <p>
                        🏪 <strong>Магазин димоходів у Тернополі “ДимоHIT”</strong> — це місце, де ви знайдете надійні рішення для безпечного та ефективного димовідведення.
                    </p>
                    <p>Ми пропонуємо:</p>
                    <ul>
                        <li>Димоходи з нержавіючої сталі (одно- і двостінні)</li>
                        <li>Сендвіч-димоходи для котлів, камінів, саун</li>
                        <li>Коаксіальні труби, вентиляційні системи та комплектуючі</li>
                        <li>Гарантія якості, сертифікація, технічна підтримка</li>
                    </ul>
                    <p>
                        🚚 <strong>Доставка по всій Україні</strong>, зручна консультація і чесні ціни — все це ви отримаєте, звернувшись до нас. Обирайте “ДимоHIT” — отримуйте <strong>надійність, довговічність та професіоналізм</strong>.
                    </p>
                </div>

                {/* Градієнт, який перекриває текст, коли він згорнутий */}
                {!expanded && <div className="store-desc-fade"></div>}
            </div>

            <button
                className="store-desc-button"
                onClick={() => setExpanded(!expanded)}
            >
                {expanded ? "Згорнути опис" : "Читати повністю"}
                <span className={`arrow ${expanded ? 'up' : 'down'}`}>›</span>
            </button>
        </section>
    );
}
